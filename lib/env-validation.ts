import "server-only";
const groups={core:["NEXT_PUBLIC_SITE_URL","NEXT_PUBLIC_SUPABASE_URL","NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY","SUPABASE_SECRET_KEY","CANDLER_ENCRYPTION_KEY_V1"],cloud:["R2_ENDPOINT","R2_ACCESS_KEY_ID","R2_SECRET_ACCESS_KEY","R2_BUCKET"],billing:["STRIPE_SECRET_KEY","STRIPE_WEBHOOK_SECRET","STRIPE_PRICE_CANDLER_PRO","STRIPE_PRICE_CLOUD_500","STRIPE_PRICE_CLOUD_1TB"],agent:["OPENAI_API_KEY","OPENAI_AGENT_MODEL"]}as const;
export function validateServerEnvironment(){const check=(keys:readonly string[])=>({configured:keys.every(key=>Boolean(process.env[key])),missing:keys.filter(key=>!process.env[key])}),core=check(groups.core),cloud=check(groups.cloud),billing=check(groups.billing),agent=check(groups.agent);const encoded=process.env.CANDLER_ENCRYPTION_KEY_V1;let encryptionKeyValid=false;try{encryptionKeyValid=Boolean(encoded&&Buffer.from(encoded,"base64").length===32)}catch{}return{core,cloud,billing,agent,encryptionKeyValid,productionReady:[core,cloud,billing,agent].every(x=>x.configured)&&encryptionKeyValid};}
