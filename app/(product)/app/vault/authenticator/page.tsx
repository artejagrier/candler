import { PageHeader } from "@/components/product/PageHeader";import { AuthenticatorClient } from "@/components/vault/AuthenticatorClient";import { getWorkspaceData } from "@/lib/data/queries";import { isSupabaseConfigured } from "@/lib/env";
export default async function AuthenticatorPage(){const data=isSupabaseConfigured?await getWorkspaceData():null;return <><PageHeader eyebrow="Vault / Authenticator" title="Authenticator" description="Encrypted TOTP seeds with server-side code generation and a clear countdown."/><AuthenticatorClient entries={(data?.authenticators??[]) as never[]}/><p className="security-note">Seeds are decrypted only long enough to generate a code and are never returned to the browser.</p></>}
